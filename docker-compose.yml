version: '3'

services:
  chromadb:
    image: chromadb/chroma:latest
    volumes:
      - ./index_data:/chroma/chroma
    ports:
      - "8000:8000"
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE

volumes:
  index_data: